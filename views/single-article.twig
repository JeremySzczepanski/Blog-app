{% extends "layout.twig" %}

{% block body %}

    <div class="" style="width: 100%;">
                                                                                    
        <img src="https://via.placeholder.com/1800x300" class="card-img-top" alt="{{ article.title }}">
        <div class="card-body">
        <h3>{{article.title}}</h3>
        <a href="">Publié le{{article.publishedAt | date("d/m/Y")}}</a>
        <p class="card-text">{{ article.content }}</p>
    
        </div>
    </div>

{% endblock %}